describe('custom commands', () => {
    it('should login using cypress custom commands', () => {
        cy.visit('http://zero.webappsecurity.com/login.html')
      //  cy.login('username', 'password')
        cy.login('username', 'password')
        
       


     

        })

       
    })